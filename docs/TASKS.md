# TASKS

| TASK-ID | Priority | Status | Description | Done Criteria | Evidence |
|---|---|---|---|---|---|
| C-RW-035 | P0 | DONE | 크레이트 이름 변경 검토 및 파급 분석 | rename 유지/보류 결정과 근거가 명시된다 | `docs/IMPLEMENTATION-PLAN.md` Decision 섹션 |
| C-RW-036 | P0 | DONE | 패키지명 하드코딩 제거(리네임 준비) | 스크립트에서 패키지명을 env 기반으로 주입 가능해야 한다 | `scripts/release_preflight.sh`, `scripts/check_security_gate.sh` (`COCLAI_PKG`) |
| C-RW-037 | P1 | DONE | 스크립트/스키마 노이즈 추가 정리 | 중복 문서 트리/벤치 바이너리 경로가 제거되고 스키마 강제관리 코드가 없어야 한다 | 삭제: `docs/` 중복 경로, `crates/coclai/src/runtime/bin/`, `crates/coclai/Cargo.toml`의 `[[bin]]`, 검색: `rg -n "SCHEMA|schema manifest|schema drift|manage_schemas"` |
| C-RW-038 | P0 | DONE | 최종 검증 | 포맷/핵심 테스트/스크립트 문법 검증 통과 | `cargo fmt --all --check`, `cargo test -p coclai runtime::{client,core,api}::tests:: -- --nocapture`, `bash -n scripts/*.sh` |
| C-RW-039 | P1 | DONE | 테스트/임시 경로의 제품명 하드코딩 축소 | `src` 내부 `coclai_` 접두 문자열이 제거되거나 정책적 예외만 남아야 한다 | 변경: `ergonomic/tests/unit.rs`, `runtime/client/tests.rs`, `domain/artifact/tests/{runtime_tasks.rs,unit_core.rs}`, `runtime/sink.rs`; 검증: `rg -n "coclai_" crates/coclai/src` (0건) |
| C-RW-040 | P0 | DONE | `runtime/api` 테스트 모듈 충돌(P0) 복구 | `runtime/api/tests.rs`와 `runtime/api/tests/mod.rs` 충돌이 제거되어 `cargo check -p coclai --tests`가 통과한다 | 삭제: `crates/coclai/src/runtime/api/tests.rs`; 검증: `cargo check -p coclai --tests` 통과 |
| C-RW-041 | P0 | DONE | stale 테스트 코드(`RuntimeConfig::new` 구 시그니처) 정리 | `runtime/api/tests.rs`의 구 시그니처 호출이 제거/이관되고 최신 API로 컴파일된다 | legacy 파일 제거: `crates/coclai/src/runtime/api/tests.rs`; 검증: `rg -n "workspace_schema_guard|RuntimeConfig::new\\(.*workspace_schema_guard" crates/coclai/src/runtime/api` (0건) |
| C-RW-042 | P0 | DONE | `WorkspaceWrite` 정책 직렬화 정합성 복구(단일 경로) | legacy `sandbox` 경로를 제거하고 `sandboxPolicy` 단일 경로에서 `writable_roots`/`network_access` 정보가 손실 없이 반영된다 | 변경: `runtime/api/wire.rs`, `runtime/api/types/policies.rs`, `runtime/rpc_contract.rs`, `runtime/api/tests/params_and_types.rs`; 검증: `cargo test -p coclai runtime::rpc_contract::tests:: -- --nocapture`, `cargo test -p coclai runtime::api::tests::params_and_types:: -- --nocapture`, `cargo test -p coclai runtime::api::tests:: -- --nocapture` |
| C-RW-043 | P0 | DONE | `Session::close` 동시성 single-flight 보장 | 동시 `close()` 호출에서도 archive RPC가 중복 수행되지 않고 결과 캐시가 일관된다 | 변경: `runtime/client/session.rs`, `runtime/client/tests.rs`(`session_close_is_single_flight_under_concurrency`); 검증: `cargo test -p coclai runtime::client::tests:: -- --nocapture` |
| C-RW-044 | P0 | DONE | `WebAdapter` clone/drop 수명주기 안전화 | 임의 clone drop이 전체 background task를 중단시키지 않으며, 소유권 규칙이 명확하다 | 변경: `adapters/web/mod.rs`, `adapters/web/tests/contract_and_spawn.rs`(`dropping_non_last_clone_keeps_background_routing_alive`); 검증: `cargo test -p coclai adapters::web::tests:: -- --nocapture` |
| C-RW-045 | P1 | DONE | 서버 요청 라우팅 유실 표면화 | `route_server_request`에서 thread/session/topic 미해결 시 유실이 로깅/메트릭으로 관측 가능하다 | 변경: `adapters/web/{handlers.rs,state.rs,mod.rs}`, 신규 테스트: `adapters/web/tests/routing_observability.rs`; 검증: `cargo test -p coclai adapters::web::tests::routing_observability:: -- --nocapture` |
| C-RW-046 | P0 | DONE | transport 입력 경계/진단 개선 | `read_line` unbounded 리스크 완화(max frame) 및 child stderr 진단 경로가 확보된다 | 변경: `runtime/transport.rs`(`max_inbound_frame_bytes`, `stderr_tail` 수집/노출); 검증: `cargo test -p coclai runtime::transport::tests:: -- --nocapture`, `cargo check -p coclai --tests` |
| C-RW-047 | P0 | DONE | timeout 계산 오버플로우 방지 | `default_timeout_ms` 변환이 안전해지고 비정상 deadline이 발생하지 않는다 | 변경: `runtime/core/dispatch.rs`(`compute_deadline_millis` saturating 변환); 검증: `cargo test -p coclai runtime::core::dispatch::tests:: -- --nocapture`, `cargo check -p coclai --tests` |
| C-RW-048 | P0 | DONE | 상태 투영 정확도 복구(cancelled + stale seq) | `turn/cancelled`가 상태 모델에 반영되고 out-of-order 이벤트가 stale로 거부된다 | 변경: `runtime/state.rs`(`TurnStatus::Cancelled`, stale seq guard); 검증: `cargo test -p coclai runtime::state::tests:: -- --nocapture`, `cargo check -p coclai --tests` |
| C-RW-049 | P1 | DONE | artifact lock stale 복구 정책 교정(cross-platform) | unix는 owner liveness 기반 stale reap을 사용하고, non-unix는 live-lock 손상 위험 없이 보수적으로 처리한다 | 변경: `domain/artifact/store.rs`, `domain/artifact/lock_policy.rs`; 검증: `cargo test -p coclai domain::artifact::lock_policy::tests:: -- --nocapture`, `cargo test -p coclai domain::artifact::tests::unit_core:: -- --nocapture` |
| C-RW-050 | P1 | DONE | artifact payload 직렬화 실패 명시적 에러화 | `to_value(...).unwrap_or(Value::Null)` 제거, 실패 시 도메인 오류가 반환된다 | 변경: `domain/artifact/execution.rs`, `domain/artifact/adapter.rs`, `domain/artifact/tests/unit_core.rs`; 검증: `cargo test -p coclai domain::artifact::tests::unit_core::build_turn_start_params_surfaces_serialization_failure -- --nocapture`, `rg -n "to_value\\(.*\\)\\.unwrap_or\\(" crates/coclai/src/domain/artifact -g'*.rs'`(0건) |
| C-RW-051 | P1 | DONE | RPC 응답 검증 정밀화 + 오류 메시지 민감정보 축소 | ID 파싱이 과도하게 허용되지 않고, 오류 메시지에 전체 payload 노출을 제한한다 | 변경: `runtime/turn_output.rs`(raw scalar fallback 제거, canonical non-empty id only), `runtime/rpc_contract.rs`(payload summary redaction); 검증: `cargo test -p coclai runtime::turn_output::tests:: -- --nocapture`, `cargo test -p coclai runtime::rpc_contract::tests:: -- --nocapture`, `cargo test -p coclai runtime::api::tests::thread_api:: -- --nocapture`, `cargo check -p coclai --tests` |
| C-RW-052 | P1 | DONE | supervisor 재시작 의미론 정교화 | clean exit와 crash를 구분하고 restart budget/reset 정책이 명확해진다 | 변경: `runtime/core/{config.rs,supervisor.rs,tests.rs}`(clean-exit 분기 + restart budget reset window), 신규 회귀: `supervisor_does_not_restart_after_clean_exit`, `supervisor_resets_restart_budget_after_stable_window`; 검증: `cargo test -p coclai runtime::core::tests::core_lifecycle:: -- --nocapture`, `cargo check -p coclai --tests` |
| C-RW-053 | P1 | DONE | sink flush 정책 성능 개선 | 이벤트당 강제 flush 병목이 완화되고 durability 정책이 문서화된다 | 변경: `runtime/sink.rs`(`JsonlFlushPolicy` 도입, default batched flush), 테스트: `runtime::sink::tests::{jsonl_file_sink_batches_flush_by_event_count,default_flush_policy_is_batched}`; 검증: `cargo test -p coclai runtime::sink::tests:: -- --nocapture`, `cargo check -p coclai --tests` |
| C-RW-054 | P1 | DONE | ergonomic 경로 처리 비손실 정책 확정 | lossy path 변환 정책을 제거하거나 명시적으로 문서화하고 테스트한다 | 변경: `ergonomic/paths.rs`(`to_string_lossy` 제거, UTF-8 only + non-lossy fallback), `ergonomic/config.rs`(정책 문서화), `ergonomic/tests/unit.rs`; 검증: `cargo test -p coclai ergonomic::paths::tests:: -- --nocapture`, `cargo test -p coclai ergonomic::tests::unit:: -- --nocapture`, `cargo check -p coclai --tests` |
| C-RW-055 | P1 | DONE | 테스트 신뢰성/이식성 정리 | panic 기반 실서버 테스트 경로 제거, timing flaky 완화, python 의존 경로 문서화/완화, 실서버 테스트는 opt-in + 사전 권한요청 원칙을 문서/스크립트에 반영 | 변경: `ergonomic/tests/real_server.rs`(panic 제거, `Result` 기반 실패 보고, `COCLAI_REAL_SERVER_APPROVED=1` opt-in), `scripts/release_preflight.sh`(python3 의존성 사전검사 + real-server approval gate); 검증: `cargo test -p coclai ergonomic::tests::real_server:: -- --list`, `bash -n scripts/release_preflight.sh`, `cargo check -p coclai --tests` |
| C-RW-057 | P0 | DONE | big-bang 전환 가드 정리 | 호환 레이어/legacy 경로/deprecated 표기가 제거되고 단일 구현 경로만 남는다 | 변경: `runtime/client/mod.rs`(deprecated alias 제거), `runtime/client/tests.rs`(alias 회귀 제거), `runtime/rpc_contract.rs`(sandboxPolicy 단일화 검증), `runtime/approvals.rs`(legacy server-request 분기 제거), `appserver/tests/mod.rs`(sandboxPolicy 단일화); 검증: `rg -n "#\\[deprecated|deprecated\\(" crates/coclai/src -g'*.rs'`(0건), `rg -n "legacy sandbox|params\\.sandbox\\b|applyPatchApproval|execCommandApproval" crates/coclai/src -g'*.rs'`(0건), `cargo test -p coclai runtime::approvals::tests:: -- --nocapture`, `cargo test -p coclai runtime::rpc_contract::tests:: -- --nocapture`, `cargo test -p coclai runtime::client::tests:: -- --nocapture`, `cargo check -p coclai --tests` |
| C-RW-056 | P0 | DONE | Option C 최종 검증/문서 동기화 | 모든 P0/P1 태스크 완료 후 전체 품질 게이트 통과 및 계획/태스크 evidence 최신화 | 검증: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace -- --skip ergonomic::tests::real_server::quick_run_executes_prompt_against_real_codex_server --skip ergonomic::tests::real_server::workflow_run_executes_prompt_against_real_codex_server`, `./scripts/check_product_hygiene.sh`, `./scripts/check_security_gate.sh`; 결과: all pass |
| C-RW-058 | P1 | DONE | CORE API 문서의 deprecated 잔여 표현 제거 | big-bang 정책과 문서가 일치하고 구 alias 명세가 문서에 재등장하지 않아야 한다 | 변경: `docs/CORE_API.md`(호환성/Deprecated 섹션 제거 후 단일 경로 정책 명시); 검증: `rg -n "continue_session|interrupt_session_turn|close_session|setup\\*" docs/CORE_API.md`(0건), `rg -n "#\\[deprecated|deprecated\\(" crates/coclai/src -g'*.rs'`(0건) |
| C-RW-059 | P0 | DONE | dispatcher server-request 경로 backpressure 정지 방지 | server request queue가 full/closed여도 dispatcher가 blocking되지 않고 timeout policy로 즉시 수습된다 | 변경: `runtime/core/dispatch.rs`(`server_request_tx.try_send` + full/closed 즉시 수습); 신규 회귀: `runtime::core::tests::server_requests::lifecycle_guards::full_server_request_queue_does_not_stall_dispatcher`; 검증: `cargo test -p coclai runtime::core::tests::server_requests::lifecycle_guards::full_server_request_queue_does_not_stall_dispatcher -- --nocapture`, `cargo test -p coclai runtime::core::tests:: -- --nocapture` |
| C-RW-060 | P0 | DONE | teardown cleanup 보장 강화 | transport terminate 실패 상황에서도 dispatcher join/pending 정리 단계가 실행된다 | 변경: `runtime/core/lifecycle.rs`(transport terminate 에러 누적 후 dispatcher join + pending resolve 항상 수행); 검증: `cargo test -p coclai runtime::core::tests:: -- --nocapture`, `cargo clippy --workspace --all-targets -- -D warnings` |
| C-RW-061 | P0 | DONE | ArtifactStore 원자 저장 계약 강제 | 기본 비원자 `save_text_and_meta` 경로가 제거되고 구현체가 원자성 전략을 명시해야 한다 | 변경: `domain/artifact/models.rs`(trait default 제거), `domain/artifact/store.rs`(FS store 명시 구현 유지); 검증: `cargo test -p coclai domain::artifact::tests:: -- --nocapture`, `cargo test --workspace -- --skip ergonomic::tests::real_server::quick_run_executes_prompt_against_real_codex_server --skip ergonomic::tests::real_server::workflow_run_executes_prompt_against_real_codex_server` |
| C-RW-062 | P0 | DONE | turn direct-output parse 실패 시 interrupt 보장 | `turn/start` 이후 direct-output normalize 실패에서도 `turn/interrupt` best-effort가 수행된다 | 변경: `domain/artifact/execution.rs`(direct-output parse error 분기 interrupt 추가), 테스트 fixture: `domain/artifact/tests.rs`; 신규 회귀: `domain::artifact::tests::runtime_tasks::run_task_sends_interrupt_when_direct_output_parse_fails`; 검증: `cargo test -p coclai domain::artifact::tests::runtime_tasks::run_task_sends_interrupt_when_direct_output_parse_fails -- --nocapture` |
| C-RW-063 | P0 | DONE | web close/session cancellation-safe 보강 | `close_session` 중간 cancellation/drop에도 lifecycle 고착 없이 rollback 된다 | 변경: `adapters/web/handlers.rs`(`SessionCloseRollbackGuard`, `ThreadArchiveRollbackGuard`), fixture 확장: `adapters/web/tests/mod.rs`; 신규 회귀: `adapters::web::tests::session_flows::close_session_cancellation_rolls_back_lifecycle`; 검증: `cargo test -p coclai adapters::web::tests::session_flows::close_session_cancellation_rolls_back_lifecycle -- --nocapture`, `cargo test -p coclai adapters::web::tests:: -- --nocapture` |
| C-RW-064 | P0 | DONE | thread/turn id 파싱 엄격화 | loose `id`/raw string fallback이 제거되어 계약 검증이 canonical field만 허용한다 | 변경: `runtime/turn_output.rs`(`id` fallback 제거), `adapters/web/wire.rs`(canonical turn id only), `runtime/api/tests/support/process_fixtures.rs`(`thread/fork` fixture canonical화), `adapters/web/tests/serialization.rs`; 검증: `cargo test -p coclai runtime::turn_output::tests:: -- --nocapture`, `cargo test -p coclai adapters::web::tests::serialization:: -- --nocapture`, `cargo test -p coclai runtime::api::tests::thread_api::typed_thread_and_turn_roundtrip -- --nocapture`, `cargo test --workspace -- --skip ergonomic::tests::real_server::quick_run_executes_prompt_against_real_codex_server --skip ergonomic::tests::real_server::workflow_run_executes_prompt_against_real_codex_server` |
| C-RW-065 | P0 | DONE | Option C 기준선 잠금: 블로커 재현 테스트와 canonical invariant 고정 | 블로커 2건이 회귀 테스트로 재현되고, invariant 목록이 계획 문서에 고정된다 | 변경: `docs/IMPLEMENTATION-PLAN.md`(Data Model/Invariant), 신규 테스트: `runtime::api::tests::run_prompt::run_prompt_lagged_thread_read_respects_absolute_deadline`, `runtime::core::tests::core_lifecycle::call_raw_abort_cleans_pending_rpc_entry`; 검증: `cargo test -p coclai runtime::api::tests::run_prompt:: -- --nocapture`, `cargo test -p coclai runtime::core::tests::core_lifecycle:: -- --nocapture` |
| C-RW-066 | P0 | DONE | `prompt_run` timeout 절대 상한 강제 | lag fallback(`thread_read`)에서도 caller timeout 초과가 발생하지 않는다 | 변경: `runtime/api/prompt_run.rs`, `runtime/api/tests/run_prompt.rs`, `runtime/api/tests/support/{mod.rs,process_fixtures.rs}`; 검증: `cargo test -p coclai runtime::api::tests::run_prompt::run_prompt_timeout_uses_absolute_deadline_under_streaming_deltas -- --nocapture`, `cargo test -p coclai runtime::api::tests::run_prompt::run_prompt_lagged_thread_read_respects_absolute_deadline -- --nocapture`, `cargo test -p coclai runtime::api::tests::run_prompt::run_prompt_recovers_when_live_stream_lags_past_terminal_event -- --nocapture` |
| C-RW-067 | P0 | DONE | `rpc_io` cancellation-safe pending cleanup | 호출 future 취소/abort 시 pending map/metric이 누수되지 않는다 | 변경: `runtime/core/rpc_io.rs`, `runtime/core/tests.rs`; 검증: `cargo test -p coclai runtime::core::tests::core_lifecycle::call_raw_abort_cleans_pending_rpc_entry -- --nocapture`, `cargo test -p coclai runtime::core::tests::core_lifecycle::call_raw_returns_timeout_when_response_missing -- --nocapture` |
| C-RW-068 | P1 | DONE | supervisor backoff sleep shutdown-interruptible화 | shutdown 시 backoff sleep 대기로 불필요한 지연이 발생하지 않는다 | 변경: `runtime/core/supervisor.rs`, `runtime/core/tests.rs`(`shutdown_interrupts_supervisor_backoff_sleep`); 검증: `cargo test -p coclai runtime::core::tests::core_lifecycle::shutdown_interrupts_supervisor_backoff_sleep -- --nocapture`, `cargo test -p coclai runtime::core::tests::core_lifecycle:: -- --nocapture`, `cargo fmt --all --check`, `cargo clippy -p coclai --all-targets -- -D warnings` |
| C-RW-069 | P0 | DONE | canonical ID parser 단일화 | thread/turn/approval id 파싱이 단일 모듈을 통하고 loose fallback이 제거된다 | 변경: `runtime/id.rs`(신규 canonical parser), `runtime/{rpc.rs,turn_output.rs}`, `adapters/web/wire.rs`, `runtime/mod.rs`; 검증: `cargo test -p coclai runtime::{id,rpc,turn_output}::tests:: -- --nocapture`, `cargo test -p coclai adapters::web::tests::serialization:: -- --nocapture`, `cargo test -p coclai runtime::api::tests::thread_api::typed_thread_and_turn_roundtrip -- --nocapture` |
| C-RW-070 | P1 | DONE | parser 호출부 통합 및 중복 모듈 삭제 | ID 파싱 중복 구현 파일/함수가 제거되고 참조가 canonical parser만 사용한다 | 변경: `runtime/rpc.rs`(legacy helper 제거), `adapters/web/wire.rs`(legacy helper 제거), `runtime/turn_output.rs`(중복 parser 제거); 검증: `rg -n "fn parse_str_field|fn parse_nested_id_field|fn parse_common_id|fn extract_canonical_id" crates/coclai/src/runtime crates/coclai/src/adapters/web -g'*.rs'`(0건), `rg -n "runtime::id::|parse_result_thread_id|parse_result_turn_id|extract_thread_id\\(" crates/coclai/src/runtime crates/coclai/src/adapters/web -g'*.rs'`, `cargo test --workspace -- --skip ergonomic::tests::real_server::quick_run_executes_prompt_against_real_codex_server --skip ergonomic::tests::real_server::workflow_run_executes_prompt_against_real_codex_server` |
| C-RW-071 | P0 | DONE | 정책 정규화/검증 단일 경로화(`sandboxPolicy`) | wire 변환과 RPC 계약 검증이 동일 규칙으로 동작하고 이중 분기가 없다 | 변경: `runtime/api/types/policies.rs`(정책 요약/검증/직렬화 단일 함수), `runtime/api/wire.rs`(보안 가드가 공용 summary 사용), `runtime/rpc_contract.rs`(request 검증이 공용 summary 사용), `runtime/api/tests/thread_api.rs`, `runtime/rpc_contract.rs` tests; 검증: `cargo test -p coclai runtime::api::types::policies::tests:: -- --nocapture`, `cargo test -p coclai runtime::rpc_contract::tests:: -- --nocapture`, `cargo test -p coclai runtime::api::tests::thread_api:: -- --nocapture`, `cargo clippy -p coclai --all-targets -- -D warnings`, `cargo test -p coclai -- --skip ergonomic::tests::real_server::quick_run_executes_prompt_against_real_codex_server --skip ergonomic::tests::real_server::workflow_run_executes_prompt_against_real_codex_server` |
| C-RW-072 | P1 | DONE | pure reducer/timeout/error projection 모듈 추출 | 상태 축약/타임아웃 계산/오류 projection이 순수 함수로 분리된다 | 변경: `runtime/state.rs`(pure reducer/retention 유지), `runtime/core/io_policy.rs`(deadline 계산, timeout payload/error payload, pending RPC outcome projection 집약), `runtime/core/dispatch.rs`(effect-only timeout 응답 위임); 검증: `cargo test -p coclai runtime::core::io_policy::tests:: -- --nocapture`, `cargo test -p coclai runtime::state::tests:: -- --nocapture`, `cargo test -p coclai runtime::core::tests:: -- --nocapture`, `cargo fmt --all --check`, `cargo clippy -p coclai --all-targets -- -D warnings` |
| C-RW-073 | P0 | DONE | turn 실행 엔진 단일화 | `prompt_run`/`artifact execution`의 중복 엔진이 제거되고 단일 엔진을 공유한다 | 변경: `runtime/turn_lifecycle.rs`(공용 turn collect/interrupt lifecycle), `runtime/api/prompt_run.rs`(공용 엔진 사용 + lag probe 유지), `domain/artifact/execution.rs`(공용 엔진 사용), `runtime/api/flow.rs`(중복 interrupt/lag enum 제거), `runtime/mod.rs`; 검증: `cargo test -p coclai runtime::api::tests::run_prompt:: -- --nocapture`, `cargo test -p coclai domain::artifact::tests::runtime_tasks:: -- --nocapture`, `cargo test -p coclai domain::artifact::tests::collect_output:: -- --nocapture`, `cargo test -p coclai runtime::turn_lifecycle::tests:: -- --nocapture`, `cargo clippy -p coclai --all-targets -- -D warnings`, `cargo test -p coclai -- --skip ergonomic::tests::real_server::quick_run_executes_prompt_against_real_codex_server --skip ergonomic::tests::real_server::workflow_run_executes_prompt_against_real_codex_server` |
| C-RW-074 | P1 | DONE | SessionKey invariant 강제(tenant/artifact/thread 정합) | thread 재사용 시 tenant/artifact 불일치를 명시적으로 거절한다 | 변경: `adapters/web/{state.rs,handlers.rs,types.rs}`, 테스트: `adapters/web/tests/session_flows.rs`(`create_session_rejects_thread_reuse_with_different_artifact`); 검증: `cargo test -p coclai adapters::web::tests::session_flows::create_session_rejects_thread_reuse_with_different_artifact -- --nocapture`, `cargo test -p coclai adapters::web::tests:: -- --nocapture`, `cargo clippy -p coclai --all-targets -- -D warnings`, `cargo test -p coclai -- --skip ergonomic::tests::real_server::quick_run_executes_prompt_against_real_codex_server --skip ergonomic::tests::real_server::workflow_run_executes_prompt_against_real_codex_server` |
| C-RW-075 | P1 | DONE | facade slim화(web/appserver) | facade는 orchestration 위임만 수행하고 핵심 규칙은 runtime service에 집중된다 | 변경: `adapters/web/service.rs`(spawn 검증/라우팅 루프 오케스트레이션 집중), `adapters/web/mod.rs`(위임 전용), `appserver/service.rs`(RPC bridge/service 집중), `appserver/mod.rs`(위임 전용); 검증: `git diff --stat -- crates/coclai/src/adapters/web/mod.rs crates/coclai/src/appserver/mod.rs`(facade net -164 lines), `cargo test -p coclai adapters::web::tests:: -- --nocapture`, `cargo test -p coclai appserver::tests:: -- --nocapture`, `cargo clippy -p coclai --all-targets -- -D warnings`, `cargo test -p coclai -- --skip ergonomic::tests::real_server::quick_run_executes_prompt_against_real_codex_server --skip ergonomic::tests::real_server::workflow_run_executes_prompt_against_real_codex_server` |
| C-RW-076 | P1 | DONE | shell 경계 정리(transport/sink/rpc/clock/fs) | 부수효과 코드와 순수 로직 경계가 명확하며 교차 참조가 줄어든다 | 변경: `runtime/core/io_policy.rs`(pure helper 집약), `runtime/transport.rs`(config/stderr/frame-trim 정책을 pure helper 위임), `runtime/sink.rs`(flush 임계값 정책을 pure helper 위임), `runtime/core/rpc_io.rs`(request/outcome projection을 pure helper 위임), `domain/artifact/lock_policy.rs`(lock parsing/reap policy 분리), `domain/artifact/store.rs`(FS effect만 유지); 검증: `rg -n "core::io_policy|mod io_policy|lock_policy|mod lock_policy" crates/coclai/src/runtime crates/coclai/src/domain/artifact`, `cargo test -p coclai runtime::transport::tests:: -- --nocapture`, `cargo test -p coclai runtime::sink::tests:: -- --nocapture`, `cargo test -p coclai runtime::core::io_policy::tests:: -- --nocapture`, `cargo test -p coclai domain::artifact::lock_policy::tests:: -- --nocapture`, `cargo test -p coclai runtime::core::tests::core_lifecycle:: -- --nocapture`, `cargo test -p coclai domain::artifact::tests::unit_core:: -- --nocapture`, `cargo clippy -p coclai --all-targets -- -D warnings`, `cargo test -p coclai -- --skip ergonomic::tests::real_server::quick_run_executes_prompt_against_real_codex_server --skip ergonomic::tests::real_server::workflow_run_executes_prompt_against_real_codex_server` |
| C-RW-077 | P1 | DONE | 테스트 계층 재편(unit/contract/integration) | 테스트 목적이 분리되고 동일 의미 중복 테스트가 제거된다 | 변경: `adapters/web/tests/mod.rs`, `appserver/tests/mod.rs`, `runtime/api/tests/mod.rs`, `domain/artifact/tests.rs`, `ergonomic/tests/mod.rs`, `plugin/tests/mod.rs`(레이어 주석/분류 고정), 신규 문서 `docs/TEST_TREE.md`(레이어 규칙/트리/실행 가이드); 검증: `./scripts/check_blocker_regressions.sh`, `cargo test -p coclai adapters::web::tests:: -- --nocapture`, `cargo test -p coclai appserver::tests:: -- --nocapture`, `cargo test -p coclai runtime::api::tests:: -- --nocapture`, `cargo test -p coclai domain::artifact::tests:: -- --nocapture`, `cargo clippy -p coclai --all-targets -- -D warnings`, `cargo test --workspace -- --skip ergonomic::tests::real_server::quick_run_executes_prompt_against_real_codex_server --skip ergonomic::tests::real_server::workflow_run_executes_prompt_against_real_codex_server` |
| C-RW-078 | P0 | DONE | 블로커 회귀 테스트 영구 고정 | timeout 상한/abort cleanup/dispatch stall 방지 핵심 회귀가 필수 세트로 고정된다 | 변경: `scripts/check_blocker_regressions.sh`(필수 3종 존재 검증 + exact 실행), `scripts/release_preflight.sh`(blocker regression gate 추가), `docs/IMPLEMENTATION-PLAN.md`(Gate/Verification 경로 고정); 검증: `bash -n scripts/check_blocker_regressions.sh scripts/release_preflight.sh`, `./scripts/check_blocker_regressions.sh`, `cargo fmt --all --check`, `cargo clippy -p coclai --all-targets -- -D warnings` |
| C-RW-079 | P1 | DONE | flaky/환경의존 테스트 정리 | timing 민감/환경 의존 테스트가 안정화되고 기본 파이프라인 변동이 줄어든다 | 변경: `appserver/tests/mod.rs`(실서버 `connect_default` 의존 제거, 테스트 전용 mock CLI 하네스 도입), `ergonomic/tests/*`(기존 opt-in/ignore 정책 유지); 검증: `for i in 1 2 3; do cargo test -p coclai appserver::tests:: -- --nocapture; done`, `for i in 1 2 3; do cargo test -p coclai ergonomic::tests::unit:: -- --nocapture; done`, `cargo test -p coclai -- --skip ergonomic::tests::real_server::quick_run_executes_prompt_against_real_codex_server --skip ergonomic::tests::real_server::workflow_run_executes_prompt_against_real_codex_server`, `cargo clippy -p coclai --all-targets -- -D warnings` |
| C-RW-080 | P1 | DONE | 파일 트리 pruning(레거시/중복 모듈 제거) | 제거 대상 모듈이 삭제되고 import graph가 단순화된다 | 삭제: `crates/coclai/src/runtime/api/ops.rs`; 변경: `runtime/api/{thread_api.rs,prompt_run.rs,wire.rs,mod.rs}`(보조 직렬화/디시리얼라이즈 함수와 `ThreadHandle` 동작 병합); 검증: `cargo check -p coclai --tests`, `cargo test -p coclai runtime::api::tests::thread_api:: -- --nocapture`, `cargo test -p coclai runtime::api::tests::run_prompt:: -- --nocapture`, `cargo clippy -p coclai --all-targets -- -D warnings`, `cargo test -p coclai -- --skip ergonomic::tests::real_server::quick_run_executes_prompt_against_real_codex_server --skip ergonomic::tests::real_server::workflow_run_executes_prompt_against_real_codex_server` |
| C-RW-081 | P1 | DONE | 문서 동기화(계획/태스크/CORE API) | 코드 구조와 문서가 불일치 없이 정합 상태를 유지한다 | 변경: `docs/IMPLEMENTATION-PLAN.md`(현재 구조/상태 스냅샷 반영), `docs/CORE_API.md`(단일 경로 계약/검증 명령 최신화), `docs/TASKS.md`(상태 동기화); 검증: `rg -n "mod ops" docs/CORE_API.md docs/IMPLEMENTATION-PLAN.md`(0건), `rg -n "turn_lifecycle|sandboxPolicy|Status Snapshot" docs/CORE_API.md docs/IMPLEMENTATION-PLAN.md`, 문서 상호 참조 수동 검토 |
| C-RW-082 | P0 | DONE | 최종 릴리즈 게이트 | fmt/clippy/test/security/product hygiene 전체 게이트가 통과한다 | 검증 통과: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`(결과: `257 passed; 0 failed; 7 ignored; 0 filtered out`), `./scripts/check_security_gate.sh`(passed), `./scripts/check_product_hygiene.sh`(passed) |
| C-RW-083 | P2 | DONE | opt-in 실서버 최종 검증 절차 문서화 | 자동 실행 없이 수동 승인 기반 실행 절차가 명확히 문서화된다 | 변경: `docs/CORE_API.md`(`Opt-In 실서버 최종 검증 절차` 섹션 추가: 승인 게이트/환경변수/직접 실행/프리플라이트 경로/정리 단계); 검증: `rg -n "Opt-In 실서버 최종 검증 절차|COCLAI_REAL_SERVER_APPROVED|COCLAI_RELEASE_INCLUDE_REAL_SERVER|release_preflight\\.sh|ergonomic::tests::real_server::" docs/CORE_API.md`, `cargo test -p coclai ergonomic::tests::real_server:: -- --list` |
| C-RW-084 | P0 | DONE | 릴리즈 산출물 완결성 정리 | 신규 core/service/doc/script 파일이 git tracked 상태로 포함되어 clean clone 기준 결과가 달라지지 않는다 | 변경: 신규 tracked 파일 `adapters/web/service.rs`, `appserver/service.rs`, `domain/artifact/lock_policy.rs`, `runtime/{id.rs,turn_lifecycle.rs}`, `runtime/core/io_policy.rs`, `scripts/check_blocker_regressions.sh`, `docs/TEST_TREE.md`; 검증: `git ls-files -- docs/TEST_TREE.md scripts/check_blocker_regressions.sh crates/coclai/src/adapters/web/service.rs crates/coclai/src/appserver/service.rs crates/coclai/src/domain/artifact/lock_policy.rs crates/coclai/src/runtime/core/io_policy.rs crates/coclai/src/runtime/id.rs crates/coclai/src/runtime/turn_lifecycle.rs`, `git status --short -- docs/TEST_TREE.md scripts/check_blocker_regressions.sh crates/coclai/src/adapters/web/service.rs crates/coclai/src/appserver/service.rs crates/coclai/src/domain/artifact/lock_policy.rs crates/coclai/src/runtime/core/io_policy.rs crates/coclai/src/runtime/id.rs crates/coclai/src/runtime/turn_lifecycle.rs` |
| C-RW-085 | P1 | DONE | 오류 표면 민감정보 노출 축소 | typed result parse/runtime internal error 경로가 raw payload/stderr contents를 직접 포함하지 않는다 | 변경: `runtime/api/wire.rs`(`deserialize_result` summary redaction), `runtime/rpc_contract.rs`(payload summary 재사용), `runtime/transport.rs`(automatic runtime error는 `child stderr tail captured`만 노출); 검증: `cargo test -p coclai runtime::api::wire::tests:: -- --nocapture`, `cargo test -p coclai runtime::transport::tests::runtime_internal_with_stderr_redacts_tail_contents -- --nocapture` |
| C-RW-086 | P1 | DONE | non-unix artifact lock fallback 보수화 | non-unix에서 unknown owner lock을 age만으로 회수하지 않아 live writer lock steal이 발생하지 않는다 | 변경: `domain/artifact/store.rs`(non-unix는 `process_is_alive` 보수 판정 사용), `domain/artifact/tests/unit_core.rs`(platform-specific regression); 검증: `cargo test -p coclai domain::artifact::tests::unit_core:: -- --nocapture` |
| C-RW-087 | P0 | DONE | loaded-thread session 경로 복구 | `start_session`/`resume_session` 이후 `Session::ask*`가 추가 `thread/resume` 없이 동일 runtime의 loaded thread로 turn을 이어가며 실서버에서 실패하지 않는다 | 변경: `runtime/api/{thread_api.rs,prompt_run.rs}`, `runtime/client/session.rs`, `runtime/client/tests.rs`; 검증: `cargo test -p coclai runtime::client::tests::session_ask_uses_loaded_thread_without_thread_resume -- --nocapture`, `cargo test -p coclai runtime::client::tests::resumed_session_ask_does_not_issue_second_thread_resume -- --nocapture`, `COCLAI_REAL_SERVER_APPROVED=1 cargo test -p coclai ergonomic::tests::real_server::resume_session_preserves_context_against_real_codex_server -- --ignored --nocapture`, `COCLAI_REAL_SERVER_APPROVED=1 cargo test -p coclai ergonomic::tests::real_server::workflow_session_preserves_context_against_real_codex_server -- --ignored --nocapture` |
| C-RW-088 | P0 | DONE | 실서버 시나리오 게이트 확장 + 릴리즈 프리플라이트 통과 | oneshot/workflow/attachment/session/resume/appserver 실사용 시나리오 6종이 opt-in live gate로 고정되고, release preflight가 live gate 포함 상태로 통과한다 | 변경: `ergonomic/tests/real_server.rs`(6개 live scenarios), `scripts/release_preflight.sh`(real_server 모듈 전체 실행), `docs/{CORE_API.md,IMPLEMENTATION-PLAN.md,TASKS.md}`; 검증: `COCLAI_REAL_SERVER_APPROVED=1 cargo test -p coclai ergonomic::tests::real_server:: -- --ignored --nocapture`, `COCLAI_REAL_SERVER_APPROVED=1 COCLAI_RELEASE_INCLUDE_REAL_SERVER=1 ./scripts/release_preflight.sh` |
| C-RW-089 | P1 | DONE | `prompt_run` hook scaffold 중복 제거 | loaded/open-or-resume 진입 경계는 유지하면서 pre/post hook orchestration 중복이 단일 helper로 수렴한다 | 변경: `runtime/api/prompt_run.rs`(`PromptRunTarget` + `run_prompt_with_hook_scaffold` + dispatch helper), 검증: `cargo test -p coclai runtime::api::tests::run_prompt:: -- --nocapture`, `cargo test -p coclai runtime::client::tests::session_ask_uses_loaded_thread_without_thread_resume -- --nocapture`, `cargo fmt --all --check`, `COCLAI_REAL_SERVER_APPROVED=1 COCLAI_RELEASE_INCLUDE_REAL_SERVER=1 ./scripts/release_preflight.sh` |
| C-RW-090 | P0 | DONE | 실서버 approval gate 추가 + live capability boundary 명시 | opt-in live gate에 approval roundtrip이 추가되고, `requestUserInput`/dynamic tool-call live 미지원 이유가 문서에 명시된다 | 변경: `ergonomic/tests/real_server.rs`(approval roundtrip + untrusted scratch dir), `docs/{CORE_API.md,IMPLEMENTATION-PLAN.md,TASKS.md,TEST_TREE.md}`; 검증: `COCLAI_REAL_SERVER_APPROVED=1 cargo test -p coclai ergonomic::tests::real_server:: -- --ignored --nocapture`, `COCLAI_REAL_SERVER_APPROVED=1 cargo test -p coclai ergonomic::tests::real_server::appserver_approval_roundtrip_executes_against_real_codex_server -- --ignored --nocapture`, `COCLAI_REAL_SERVER_APPROVED=1 COCLAI_RELEASE_INCLUDE_REAL_SERVER=1 ./scripts/release_preflight.sh` |
