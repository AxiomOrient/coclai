# codex-runtime 0.6.0

## Summary

- cleaned the release documentation surface around the root `SPEC.md`
- removed legacy rename-audit and completed planning artifacts from the release tree
- added hard randomized stress coverage for prompt execution and multi-tenant web session flows

## Validation

- `./scripts/release_preflight.sh`
- `cargo publish -p codex-runtime --dry-run --allow-dirty`

## Highlights

- prompt execution now has repeated user-like mixed-path coverage over high-level and typed entrypoints
- web adapter flow coverage now includes repeated randomized multi-tenant session traffic and cross-tenant denial checks
- localized docs now point at durable sources instead of removed migration-only documents
